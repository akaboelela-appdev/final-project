<!-- Validation failure messages -->
<% if @priority.present? %>
  <% if @priority.errors.any? %>
    <% @priority.errors.full_messages.each do |message| %>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% end %>
  <% end %>
<% end %>

<h1>
  Add a new priority
</h1>

<hr>

<form action="/create_priority" method="post">
  <!-- Label and input for name -->
  <div class="form-group">
    <label for="name">
      Name
    </label>

    <input type="text" id="name" name="name" class="form-control" value="<%= @priority.try(:name) %>">
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Create priority
  </button>
</form>

<hr>

<a href="/priorities">
  Go back
</a>
